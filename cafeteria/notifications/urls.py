from django.urls import path
from . import views

app_name = 'notifications'

urlpatterns = [
    # URLs específicas para notificaciones
    #path('', views.notification_list, name='lista'),
    # Más URLs...
]